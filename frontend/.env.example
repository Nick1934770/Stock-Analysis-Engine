# Copy to .env.local for local overrides

# Backend REST URL (leave empty to use Vite proxy in dev)
VITE_API_URL=

# Backend WebSocket URL (leave empty to use Vite proxy in dev)
# In production set to: wss://your-backend.onrender.com
VITE_WS_URL=
